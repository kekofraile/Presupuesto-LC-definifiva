# Assets

- `img/`: logotipos de marcas, iconografía y recursos exportados desde la herramienta de diseño.
- `styles/`: variables de color, tipografía y tokens de diseño usados para personalizar la PWA offline.

Los assets se empaquetan con la app y se cachean mediante el Service Worker para garantizar disponibilidad sin conexión.
